{
    "name": "algorave-hub",
    "version": "1.0.0",
    "description": "Algorave Central Hub",
    "scripts": {
        "postinstall": "npm install --prefix web && npm install --prefix strudel-mcp-server",
        "start": "npm run dev --prefix web",
        "start:prod": "npm run start --prefix web",
        "docker:up": "docker-compose up -d --build",
        "docker:down": "docker-compose down",
        "docker:logs": "docker-compose logs -f",
        "mcp": "docker-compose up -d --build mcp-server",
        "web": "docker-compose up -d --build web-app",
        "build-docs": "node scripts/sync-docs.js && mmdc -i doc/architecture.mmd -o web/public/doc/architecture.png -t dark -b transparent && docker-compose run --rm docs && node scripts/sync-examples.js",
        "build": "npm run build-docs && npm run build --prefix strudel-mcp-server && npm run build --prefix web"
    },
    "private": true,
    "devDependencies": {
        "@mermaid-js/mermaid-cli": "^11.12.0"
    }
}