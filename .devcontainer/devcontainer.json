{
	"name": "Algorave Hub",
	"image": "mcr.microsoft.com/devcontainers/typescript-node:1-22-bookworm",
	"features": {
		"ghcr.io/devcontainers/features/docker-in-docker:2": {
			"version": "latest",
			"enableNonRootDocker": "true",
			"moby": "true"
		},
		"ghcr.io/devcontainers/features/git-lfs:1": {}
	},
	// Forward web app and MCP server ports
	"forwardPorts": [
		3001,
		3000,
		8080,
		4321
	],
	// Helper extensions
	"customizations": {
		"vscode": {
			"extensions": [
				"esbenp.prettier-vscode",
				"dbaeumer.vscode-eslint"
			]
		}
	},
	// Install project dependencies and init submodules
	"postCreateCommand": "git submodule update --init --recursive && npm install && cd web && npm install && cd .. && cp -av .env.example .env && npm install -g @google/gemini-cli@latest && mkdir -p ~/.gemini && cp .devcontainer/gemini-settings.json ~/.gemini/settings.json"
}